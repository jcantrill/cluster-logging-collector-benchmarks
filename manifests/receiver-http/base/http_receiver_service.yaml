apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: logging-benchmarking
    app.kubernetes.io/component: receiver
  name: http-receiver
spec:
  ports:
  - port: 8090
    protocol: TCP
  selector:
    app.kubernetes.io/name: logging-benchmarking
    app.kubernetes.io/component: receiver